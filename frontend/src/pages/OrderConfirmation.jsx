const OrderConfirmation = () => {
  return (
    <div className="container mt-5">
      <h2>Order Confirmation Screen (we will build in STEP 10 after PayPal)</h2>
    </div>
  );
};

export default OrderConfirmation;
